```sh
# Make sure to have poetry installed
poetry --version

# Export ENV variables
export URL=<URL HERE>

# Setup project
source setup.sh
# Install poetry and dependencies
make install
# Run
make run
```
